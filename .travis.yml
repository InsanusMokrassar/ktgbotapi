language: java
install: true

os: linux
dist: trusty
jdk: oraclejdk8

jobs:
  include:
    - stage: build
      script: ./gradlew build -s -x jvmTest -x jsTest -x jsNodeTest -x jsBrowserTest
    - state: test
      script: ./gradlew allTests
